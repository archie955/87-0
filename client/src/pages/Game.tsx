import AppLayout from "@/layout/AppLayout";
import TeamSelect from "@/components/TeamSelect";

const Game = () => {
  return (
    <AppLayout>
      <TeamSelect />
    </AppLayout>
  );
};

export default Game;
