import AppLayout from "@/layout/AppLayout";
import DashboardContent from "@/components/DashboardContent";

const Home = () => {
  return (
    <AppLayout>
      <DashboardContent />
    </AppLayout>
  );
};

export default Home;
