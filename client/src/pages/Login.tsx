import AppLayout from "@/layout/AppLayout";
import AuthCard from "@/components/auth-card";

const Login = () => {
  return (
    <AppLayout>
      <AuthCard />
    </AppLayout>
  );
};

export default Login;
