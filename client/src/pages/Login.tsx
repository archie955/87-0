import AppLayout from "@/layout/AppLayout";
import AuthCard from "@/components/AuthCard";

const Login = () => {
  return (
    <AppLayout>
      <AuthCard />
    </AppLayout>
  );
};

export default Login;
